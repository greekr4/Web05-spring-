<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mpper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.myshop.BasketMapper">

 
	<!-- 장바구니 조회 -->
	<select id="BasketMapper" resultType="com.myshop.dto.BasketDTO" >
		select a.s_img_desc ,a.seq as 'p_seq',b.* from v_product a
		inner join logmaster b 
		on(a.pcode=b.value)
		where b.cus_seq = #{cus_seq}
		group by b.value
		order by b.seq desc
	</select>
	


	

	

</mapper>
